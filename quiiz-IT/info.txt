
-- Info on what will be needed for running the app

    front end dependencies
        
        -- react
        -- react-dom
        -- react-router-dom
        -- react-icons
        -- tailwindcss
        -- postcss
        -- autoprefixer
        -- Googlefonts --play and playball
            ---npm install @fontsource/play @fontsource/playball

    Backend dependencies
        -- express
        -- mongoose
        -- cors
        -- dotenv
        -- (optional) nodemon
        -- (optional) mongo Atlas


to improve
--height on phones is a  little bid
--when you share whilst on a page like tamati..it will throw an error
--seo stuff


    